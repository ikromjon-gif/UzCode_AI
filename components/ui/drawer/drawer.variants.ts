import { cva } from "class-variance-authority";

import { drawerContentBase } from "./drawer.styles";

export const drawerContentVariants = cva(drawerContentBase, { variants: {}, defaultVariants: {} });
