import { cva } from "class-variance-authority";

import { tooltipContentBase } from "./tooltip.styles";

export const tooltipContentVariants = cva(tooltipContentBase, { variants: {}, defaultVariants: {} });
