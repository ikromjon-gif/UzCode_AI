import { cva } from "class-variance-authority";

import { popoverContentBase } from "./popover.styles";

export const popoverContentVariants = cva(popoverContentBase, { variants: {}, defaultVariants: {} });
