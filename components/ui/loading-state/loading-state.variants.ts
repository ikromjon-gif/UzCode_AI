import { cva } from "class-variance-authority";

import { loadingStateBase } from "./loading-state.styles";

export const loadingStateVariants = cva(loadingStateBase, { variants: {}, defaultVariants: {} });
