import { cva } from "class-variance-authority";

import { breadcrumbListBase } from "./breadcrumb.styles";

export const breadcrumbVariants = cva(breadcrumbListBase, { variants: {}, defaultVariants: {} });
