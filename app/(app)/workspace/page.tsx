import { WorkspaceExplorerShell } from "@/features/workspace-explorer";

/**
 * UzCode AI — /workspace route
 * Sprint 5 replaces the Sprint 4 RouteSlotPlaceholder here with the
 * real (foundation-only) Workspace Explorer shell.
 */
export default function WorkspacePage() {
  return <WorkspaceExplorerShell />;
}
