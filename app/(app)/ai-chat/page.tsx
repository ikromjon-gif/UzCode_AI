import { AiChatShell } from "@/features/ai-chat";

/**
 * UzCode AI — /ai-chat route
 * Sprint 7 replaces Sprint 4's RouteSlotPlaceholder here with the
 * real (foundation-only) AI Chat Workspace.
 */
export default function AiChatPage() {
  return <AiChatShell />;
}
